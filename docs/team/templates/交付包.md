# 交付包 d-YYYY-MM-DD-N

> 由 Dispatcher 填写，用户确认后交给 Executor。逐项写清，不留模糊空间。

## 目标

- 要解决的问题 / 要交付的行为（一句话 + 验收行为列表）。

## 非目标

- 明确不做的事。

## 允许修改的范围

- 涉及的文件/包/接口（精确到文件或函数）。
- 禁止改动的清单（如 `docs/Note.md`、`.env`、既有 CLI 行为、`core/models.py` 公开 API 等）。

## 必须证明的行为 + 反例

- 行为 1：输入 X → 期望输出 Y；反例：输入 X' → 必须拒绝/报错。
- 行为 2：……

## 验证命令

```bash
make check
make test
<针对性命令>
```

## 证据要求

- 测试名称 + 关键断言；产物/报告路径；原始命令输出。

## 是否需要真实实验

- 不需要 / 需要（给出配置、命令、通过标准与准入条件）。
