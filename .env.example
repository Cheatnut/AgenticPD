# AgenticPD sensitive config template: copy to .env and fill in real values (.env is gitignored)
# DeepSeek API key (required for real optimization mode; not needed for --dry-run)
DEEPSEEK_API_KEY=YOUR-API-KEY
